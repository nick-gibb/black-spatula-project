# ingestion/web_scraper.py
def scrape_web_articles():
    # Placeholder: Return mock articles
    # Real implementation would scrape a web page (e.g., using requests + BeautifulSoup)
    return [
        {'type': 'html', 'content': '<html><body><p>Scraped article text</p></body></html>', 'metadata': {'source': 'web'}}
    ]