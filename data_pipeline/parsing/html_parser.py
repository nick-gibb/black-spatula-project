# parsing/html_parser.py
def parse_html(html_content):
    # Placeholder logic
    # Real logic might use BeautifulSoup to strip tags, extract text, and figure references
    text = "Extracted HTML text (placeholder)"
    figures = []
    return text, figures